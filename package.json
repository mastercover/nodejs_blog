{
  "name": "blog",
  "version": "1.0.0",
  "description": "blog",
  "main": "src/app.js",
  "scripts": {
    "beautiful": "prettier --single-quote --trailing-comma all --tab-width 4 --write 'src/**/*.{js,json,scss}'",
    "start": "nodemon --inspect src/app.js",
    "watch": "node-sass --watch src/resources/scss/app.scss src/public/css/app.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mastercover/nodejs_blog.git"
  },
  "keywords": [
    "blog",
    "blogger",
    "nodejs"
  ],
  "author": "master cover",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mastercover/nodejs_blog/issues"
  },
  "homepage": "https://github.com/mastercover/nodejs_blog#readme",
  "lint-staged":
  {
    "src/**/*.{js,json,scss}": "prettier --single-quote --trailing-comma all --tab-width 4 --write"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-handlebars": "^6.0.6"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "morgan": "^1.10.0",
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.1"
  }
}
